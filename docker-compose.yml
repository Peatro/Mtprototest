services:
  postgres:
    image: postgres:15
    container_name: mtproto-postgres
    environment:
      POSTGRES_DB: mtproto_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"